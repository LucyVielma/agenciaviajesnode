CREATE TABLE `agenciaviajes`.`testimoniales` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `nombre` varchar(60) NOT NULL,
  `correo` varchar(60) NOT NULL,
  `mensaje` TEXT NOT NULL,
  PRIMARY KEY (`id`)
);
